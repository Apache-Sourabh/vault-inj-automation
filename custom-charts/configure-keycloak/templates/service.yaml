apiVersion: v1
kind: Service
metadata:
  name: {{ include "configure-keycloak.fullname" . }}
  labels:
    {{- include "configure-keycloak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "configure-keycloak.selectorLabels" . | nindent 4 }}
