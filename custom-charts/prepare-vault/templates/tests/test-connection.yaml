apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prepare-vault.fullname" . }}-test-connection"
  labels:
    {{- include "prepare-vault.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prepare-vault.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
