apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "setup-vault-oidc.fullname" . }}-test-connection"
  labels:
    {{- include "setup-vault-oidc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "setup-vault-oidc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
