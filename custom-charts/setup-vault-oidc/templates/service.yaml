apiVersion: v1
kind: Service
metadata:
  name: {{ include "setup-vault-oidc.fullname" . }}
  labels:
    {{- include "setup-vault-oidc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "setup-vault-oidc.selectorLabels" . | nindent 4 }}
