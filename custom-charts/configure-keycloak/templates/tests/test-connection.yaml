apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "configure-keycloak.fullname" . }}-test-connection"
  labels:
    {{- include "configure-keycloak.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "configure-keycloak.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
