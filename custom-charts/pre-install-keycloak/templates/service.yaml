apiVersion: v1
kind: Service
metadata:
  name: {{ include "pre-install-keycloak.fullname" . }}
  labels:
    {{- include "pre-install-keycloak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pre-install-keycloak.selectorLabels" . | nindent 4 }}
